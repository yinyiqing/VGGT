# manual_views

这里放手动查看 3D 结果时保存的截图。

建议文件名：

```text
meshlab_scene_conf50_overview.png
meshlab_scene_conf10_noisy.png
```

当前截图：

- `meshlab_scene_conf50_overview.png`：当前推荐展示结果。
- `meshlab_scene_conf10_noisy.png`：低阈值噪声对照。

截图要求：

- PNG/JPG 格式。
- 能看清主体和噪声差异。
- 不放 `.ply`、`.glb`、`.npz`。
